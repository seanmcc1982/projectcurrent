<% provide(:title, "Films") %>
<p id="notice"><%= notice %></p>

<h1>Films</h1>

<br><br>

Title | Filmcast | Plot | Poster url | Trailer url | Runtime

<br><br>

    <% @films.each do |film| %>
        <%= film.title %> | <%= film.filmcast %> | <%= film.plot %> | <%= film.poster_url %> | <%= film.trailer_url %> | <%= film.runtime %>  
        
        <% if user_signed_in? || admin_signed_in? %>
            <%= link_to 'Show', film %> |
        <% end %>
        <% if admin_signed_in? %>
            <%= link_to 'Edit', edit_film_path(film) %> | <%= link_to 'Destroy', film, method: :delete, data: { confirm: 'Are you sure?' } %>
        <% end %>
        <br>
    <% end %>
<br><br>
<% if admin_signed_in? %>
    <%= link_to 'New Film', new_film_path %>
<% end %>